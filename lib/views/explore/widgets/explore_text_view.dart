


import 'package:flutter/material.dart';

class ExploreTextView extends StatelessWidget {
  const ExploreTextView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
            child: Text(
              'My Card',
              style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
            ),
          );
  }
}